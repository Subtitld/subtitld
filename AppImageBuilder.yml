version: 1
script:
  - rm -rf AppDir | true
  - ls dist
  - mv dist/subtitld AppDir
  #   - python3.10 -m pip install --upgrade --force-reinstall --prefix=/usr --root=AppDir .
  #   - python3.10 -m pip install . --prefix=/usr --root=AppDir
  - mkdir -p AppDir/usr/share/icons/hicolor/256x256/apps
  - cp snap/gui/icon.png AppDir/usr/share/icons/hicolor/256x256/apps/org.jonata.Subtitld.png
  


AppDir:
  path: ./AppDir

  # after_bundle: |
  #   # Set python 3.10 env
  #   export PYTHONHOME=${APPDIR}/usr
  #   export PYTHONPATH=${APPDIR}/usr/lib/python3.10/site-packages:$APPDIR/usr/lib/python3.10
  #   export PATH=${APPDIR}/usr/bin:$PATH
  #   # Set python 3.10 as default
  #   ln -fs python3.10 $APPDIR/usr/bin/python3

  app_info:
    id: org.jonata.Subtitld
    name: Subtitld
    icon: org.jonata.Subtitld
    version: 22.09.01.01
    exec: "subtitld/subtitld"
    # exec_args: "$APPDIR/subtitld $@"
    exec_args: "$@"

  apt:
    arch: amd64
    sources:
      - sourceline: 'deb [arch=amd64] http://archive.ubuntu.com/ubuntu/ jammy main restricted universe multiverse'
        key_url: 'http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x871920D1991BC93C'

    include:
      - python3
      - python3-pkg-resources
      #   - libffms2-5
      #   - libmpv1
      # - libopengl0
      # - libssl1.1
      # - libk5crypto3
      # - libkrb5-3
      # - libgssapi-krb5-2

  runtime:
    env:
      PYTHONHOME: '${APPDIR}/usr'
      PYTHONPATH: '${APPDIR}/usr/lib/python3.10/site-packages'


AppImage:
  update-information: 'gh-releases-zsync|AppImageCrafters|subtitld|latest|Subtitld-*x86_64.AppImage.zsync'
  sign-key: None
  arch: x86_64
